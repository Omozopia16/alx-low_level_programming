#include <stdir.h>
/**
 * main -  a program that prints all single digit numbers of base
 *
 * Return: always 0
 */
int main(void)
{
	char i;

	for (i = 0 ; i < 10 ; i++)
		putchar(i);
	putchar(\n);
	return (0);
}
